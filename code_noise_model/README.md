# Noise Model Design Tool

This isolated package simulates and fits implicit visuomotor adaptation noise
models to evaluate which 200-trial designs best identify process-noise
structure.

Run the end-to-end shortlist analysis from this folder:

```bash
python examples/run_design_shortlist.py
```

Outputs are written to `outputs/` and the model summary report is written to
`reports/`.
